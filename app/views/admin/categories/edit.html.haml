%h1 Category edition

- semantic_form_for @category, :url => admin_category_path do |f|
  - f.inputs do
    = f.input :name
  - f.buttons do
    = f.commit_button t('apply')
    
= link_to t('back'), admin_categories_path