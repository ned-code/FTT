<h1><%= t('.title') %></h1>

<%= render :partial => 'layouts/flash' %>

<ul>
  <% @categories.each do |category| %>
    <li>
      <%= "#{category.name}" %>
      |
      <%= link_to t('edit'), edit_admin_category_path(category) %>
      <%= link_to t('destroy'), admin_category_path(category), :confirm => I18n.t('destroy_confirmation'), :method => :delete %>
    </li>
  <% end %>
</ul>

<%= link_to t('.add'), new_admin_category_path %>
|
<%= link_to t('admin.home.index.title'), admin_path %>
