%h1 Add new category

- semantic_form_for @category, :url => admin_categories_path  do |f|
  - f.inputs do
    = f.input :name

  - f.buttons do
    = f.commit_button t('add')

= link_to t('back'), admin_categories_path
