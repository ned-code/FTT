<h1>Users listing</h1>

<ul id="user-list">
  <% @users.each do |user| %>
    <li>
      <%= "#{user.first_name} #{user.last_name} (#{user.email}) / last login: #{user.last_sign_in_at}" %>
    </li>
  <% end %>
</ul>

<%= link_to t('admin.home.index.title'), admin_path %>
